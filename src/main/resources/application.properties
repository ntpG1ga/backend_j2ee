# ====================== SERVER & DATABASE ======================
server.port=7000
spring.datasource.url=jdbc:mysql://localhost:3306/bus_ticket_booking?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=

# ====================== EMAIL ======================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hahung04022004@gmail.com
spring.mail.password=auwmfalrkvxeizwz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ====================== OLLAMA LOCAL (ĐÃ FIX LỖI model is required) ======================
spring.ai.ollama.base-url=http://localhost:11434


spring.ai.ollama.chat.options.model=qwen2.5:0.5b
spring.ai.ollama.embedding.options.model=nomic-embed-text
# spring.ai.ollama.chat.options.num-gpu=0

# ====================== PINECONE (ĐÃ FIX LỖI DNS) ======================
# Dùng endpoint chính xác + thêm timeout
spring.ai.vectorstore.pinecone.api-key=${PINECONE_API_KEY}
spring.ai.vectorstore.pinecone.project-id=${PINECONE_PROJECT_ID}
spring.ai.vectorstore.pinecone.environment=${PINECONE_ENVIRONMENT}
spring.ai.vectorstore.pinecone.index-name=rag-docs

# Thêm 2 dòng này để tránh lỗi mạng ở VN
spring.ai.vectorstore.pinecone.endpoint=https://rag-docs-${PINECONE_PROJECT_ID}.svc.${PINECONE_ENVIRONMENT}.pinecone.io
spring.ai.vectorstore.pinecone.metadata-config.enabled=true

# ====================== LOG ======================
logging.level.org.springframework.ai=INFO
logging.level.com.example.doan_j2ee=INFO